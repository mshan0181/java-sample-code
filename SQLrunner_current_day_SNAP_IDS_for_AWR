pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "parentJob"
            }
        }
        stage('triggerChildJob') {
            steps {
                build job: "SQL_runner_AWR_report_in_Oracle", wait: true
            }
        }
    }
}
